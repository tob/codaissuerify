<div class="col-md-3">
  <h4><%= artist.name %></h4>
  <%= image_tag(artist.image_url) =%>
  <%= artist.id =%>
  <%= link_to "Delete", artist_path(@artist), class: "btn btn-default", method: :delete, data: { confirm: "Are you sure?" } %>
</div>
