<%= form_for [@artist, @artist.songs.new] do |f| %>

  <% @artist.errors.full_messages.each %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :length %>
  <%= f.text_field :length %>

<%= f.hidden_field :artist_id, value: @artist.id %>

  <%= f.submit "Create" %>
  <% end %>
